services:
  app:
    image: ${IMAGE_TAG}
    container_name: abet-staging
    environment:
      ENVIRONMENT_NAME: ${ENVIRONMENT_NAME:-staging}
      SPRING_PROFILES_ACTIVE: staging
      SPRING_DATASOURCE_URL: jdbc:mariadb://database:3306/abet_staging
      SPRING_DATASOURCE_USERNAME: abet_user
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      JAVA_OPTS: -Xmx512m -Xms256m -XX:+UseG1GC -XX:+UseContainerSupport
      SERVER_FORWARD_HEADERS_STRATEGY: framework
    ports:
      # Staging on port 8081, bound to Tailscale IP only
      - "${TAILSCALE_IP:-100.79.13.76}:8081:8080"
    networks:
      - abet-network-staging
    depends_on:
      database:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:8080/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 768M
        reservations:
          cpus: '0.5'
          memory: 512M

  database:
    image: mariadb:11.4
    container_name: abet-db-staging
    environment:
      MARIADB_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MARIADB_DATABASE: abet_staging
      MARIADB_USER: abet_user
      MARIADB_PASSWORD: ${DB_PASSWORD}
      MARIADB_AUTO_UPGRADE: 1
    ports:
      - "${TAILSCALE_IP:-100.79.13.76}:3307:3306"
    volumes:
      - db_data_staging:/var/lib/mysql
      - ./database/init:/docker-entrypoint-initdb.d:ro
    networks:
      - abet-network-staging
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 256M

  # E2E test runner service (use with --profile e2e)
  playwright-tests:
    build:
      context: .
      dockerfile: Dockerfile.playwright
    container_name: abet-e2e-tests
    profiles:
      - e2e
    environment:
      # Point to staging app service
      PLAYWRIGHT_BASE_URL: http://app:8080
      # Enable debug output
      DEBUG: ${DEBUG:-pw:api}
      # CI mode
      CI: 'true'
    volumes:
      # Mount test results for retrieval
      - ./frontend/test-results:/app/test-results
      - ./frontend/playwright-report:/app/playwright-report
      # Mount the test files (for development)
      - ./frontend/e2e:/app/e2e:ro
      - ./frontend/playwright.staging.config.ts:/app/playwright.staging.config.ts:ro
    networks:
      - abet-network-staging
    depends_on:
      app:
        condition: service_healthy
    command: >
      sh -c "
        echo 'Waiting for staging server to be ready...' &&
        wget --spider --quiet --tries=30 --waitretry=2 http://app:8080/actuator/health &&
        echo 'Staging server is ready, starting tests...' &&
        npx playwright test --config=playwright.staging.config.ts --reporter=list,html,junit
      "

volumes:
  db_data_staging:
    driver: local

networks:
  abet-network-staging:
    driver: bridge